{
  "name": "it-links-dashboard",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npx concurrently@9 -k -n web,api -c cyan,magenta \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "npm run dev -w frontend",
    "dev:api": "npx wrangler@4 pages dev --port 8788 --proxy 5173",
    "build": "npm run build -w frontend",
    "deploy": "npm run build && npx wrangler@4 pages deploy frontend/dist",
    "db:migrate:local": "npx wrangler@4 d1 migrations apply it-links-db --local",
    "db:migrate:remote": "npx wrangler@4 d1 migrations apply it-links-db --remote"
  }
}
